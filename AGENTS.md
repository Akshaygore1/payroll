<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

For new or modified UI code, use Client Components only.
Do not create or use Server Components.
Do not create or use Server Actions.
Create API routes under `/api` for server-side or external API calls.
For the home/index route, use only the chat scene.
Do not introduce a new design style.
Use the current design system and existing design patterns only.
Use Shadcn components only when they fit the current design system.
<!-- END:nextjs-agent-rules -->
